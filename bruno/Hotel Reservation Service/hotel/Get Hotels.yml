info:
  name: Get Hotels
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/hotel/list?page=0&size=10"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get Reservation
